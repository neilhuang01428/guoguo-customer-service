# 內部規範（不對外公開）
html-guidelines.html
content-quality-guidelines.html
# 設計樣張草稿
_style-samples/
.DS_Store

# 內部規劃文件（給自己看的，不對外公開，不上 GitHub）
_planning/

# 商品原始匯出檔（含 sku/庫存，內部 build 用，別上公開站）
data/*-export.csv

# 無導外版建置輸出（Cloudflare Pages 端產生，別追蹤）
dist/

# 中性版首頁：由 build-neutral.sh 在部署時（step 0）即時重生，不再 commit（避免與 articles.json 不同步）
index.neutral.html
